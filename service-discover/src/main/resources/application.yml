spring:
  profiles:
    active: dev
  application:
    name: 'service-discover'
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: ${PORT:8761}
